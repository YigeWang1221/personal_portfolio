# F1 Race Prediction with Hierarchical Bayesian Modeling — staged assets

**Staged:** 2026-09-24 (ADR-011, ADR-015). Diagram sources (`.mmd`) are rendered to `.svg` with `npm run diagrams`; both are committed.

| File | Label | What it shows | Used on | Caveats |
|---|---|---|---|---|
| `assets/model.mmd` → `.svg` | CURRENT | Structure of the final model | "The model"; plan-and-design thumbnail | Derived from the model-building notebook |
| `assets/pipeline.mmd` → `.svg` | CURRENT | Data → model → simulation → evaluation | "The question and the approach" | — |
| `assets/team_tiers.png` | — (result plot) | Finishing-position distributions by team tier, used to set priors | "The model" | Copied from the public repository; public F1 data only |
| `assets/abu_dhabi_position_distribution.png` | — (result plot) | Pre-race position distributions for the 2025 finale | "From posterior to finishing orders" | Copied from the public repository; public F1 data only |
