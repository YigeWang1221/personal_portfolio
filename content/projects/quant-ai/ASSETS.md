# Quant AI — staged assets

**Staged:** 2026-09-24 (ADR-011, ADR-015). Diagram sources (`.mmd`) are rendered to `.svg` with `npm run diagrams`; both are committed.

| File | Label | What it shows | Used on | Caveats |
|---|---|---|---|---|
| `assets/pipeline.mmd` → `.svg` | CURRENT | Data, labels, model, walk-forward training, evaluation | "Pipeline" | No performance plots are used (owner instruction) |
